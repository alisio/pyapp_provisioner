---
# roles/python_app/molecule/default/requirements.yml
# Dependencies for molecule tests

roles:
  - name: mambaorg.micromamba
    version: main

collections:
  - name: community.general
    version: ">=6.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
  - name: community.docker
    version: ">=3.0.0"
